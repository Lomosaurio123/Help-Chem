import 'package:flutter/material.dart';

class PercentagesPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Este es la opcion de porcentajes'),
    );
  }
}