import 'package:flutter/material.dart';


class MatterBalanacePage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Balance de materia'),
      ),

      body: Center(
        child: Text('Cuerpo del balance de materia'),
      ),

    );
  }
}